name: Create IAM User and Policy v1

on: 
  push:
    branches:
      - master  

jobs:
  create-iam-user-and-policy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: us-east-1

    - name: Create IAM User
      run: |
        USER_NAME=My-user2
        if aws iam get-user --user-name $USER_NAME; then
          echo "User $USER_NAME already exists"
        else
          aws iam delete-user --user-name $USER_NAME
        fi
        aws iam create-user --user-name $USER_NAME

    - name: Create IAM Policy
      run: |
        cat <<EoF > access-policy.json
        {
          "Version": "2012-10-17",
          "Statement": [
            {
              "Effect": "Allow",
              "Action": [
                "s3:ListBucket",
                "s3:GetObject",
                "s3:PutObject"
              ],
              "Resource": "*"
            }
          ]
        }
        EoF

        POLICY_NAME=My-Policy1

        aws iam create-policy --policy-name $POLICY_NAME --policy-document file://access-policy.json

    - name: Attach IAM Policy to User
      run: |
        # POLICY_ARN=$(`aws iam list-policies --output json --query 'Policies[0].Arn' --output text`)
        POLICY_ARN=$(aws iam list-policies --query "Policies[?PolicyName=='$POLICY_NAME'].Arn" --output text)
        aws iam attach-user-policy --user-name $USER_NAME --policy-arn $POLICY_ARN

    # - name: Create Access Key for IAM User
    #   run: |
    #     aws iam create-access-key --user-name My-user > access-keys.json
        
